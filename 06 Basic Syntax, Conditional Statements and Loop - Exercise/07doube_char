command = input()

while command != "End":
    currentPrint = ""
    if command != "SoftUni":
        for i in command:
            currentPrint += i * 2
        print((currentPrint))
    command = input()
